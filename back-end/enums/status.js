/**
 * Defines and exports the status constants.
 * These constants represent the possible status values.
 */
const status = {
    ACTIVE: 'ACTIVE',    // Represents an active status
    INACTIVE: 'INACTIVE' // Represents an inactive status
};

// Export the status constants for use in other modules
module.exports = status;
