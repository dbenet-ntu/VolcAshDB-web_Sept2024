/**
 * Defines and exports the types constants.
 * These constants represent the possible types values.
 */
const types = {
    NATURAL: 'natural',             // Represents natural particle
    EXPERIMENTAL: 'experimental'    // Represents experimental particle
};

// Export the types constants for use in other modules
module.exports = types;
