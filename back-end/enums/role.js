/**
 * Defines and exports the possible role values.
 * These constants represent the allowed role options.
 */
const role = {
    USER: 'user',               // user role
    ANNOTATOR: 'annotator',     // annotator role
    TEAM_MEMBER: 'team member', // team member role
    ADMIN: 'admin',             // admin role
};

// Export the role constants for use in other modules
module.exports = role;
